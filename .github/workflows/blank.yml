# # name: Metrics
# on:
#   # Terpicu otomatis setiap Anda melakukan push ke branch main
#   push:
#     branches: ["main"]
#   # Terpicu otomatis setiap 24 jam sekali (pukul 00:00 UTC)
#   schedule:
#     - cron: "0 0 * * *"
#   # Memungkinkan Anda menjalankan workflow secara manual dari tombol di tab Actions
#   workflow_dispatch:

# jobs:
#   github-metrics:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#     steps:

#       # Langkah 1: Menghasilkan Grafik Statistik Utama (stats.svg)
#       - name: Generate Main Stats
#         uses: lowlighter/metrics@latest
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
#           output_action: push
#           committer_branch: stats-output
#           filename: stats.svg
#           base: header, activity, community, repositories, metadata
#           config_timezone: Asia/Makassar
#           theme: tokyonight

#       # Langkah 2: Menghasilkan Grafik Bahasa Paling Sering Digunakan (languages.svg)
#       - name: Generate Top Languages
#         uses: lowlighter/metrics@latest
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
#           output_action: push
#           committer_branch: languages-output
#           filename: languages.svg
#           base: ""
#           plugin_languages: yes
#           plugin_languages_ignored: html, css  # Menyembunyikan HTML/CSS agar kalkulasi bahasa logika lebih presisi
#           plugin_languages_details: bytes-size, percentage
#           plugin_languages_limit: 8
#           config_timezone: Asia/Jakarta
#           theme: tokyonight
